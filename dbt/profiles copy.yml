dbt_epc:
  outputs:
    dev:
      account: # Account > Connect a tool to Snowflake > Account Identifier (specifically the one with - (hyphen))
      database: EPC_DB
      password: epc_dbt#123
      role: TRANSFORM
      schema: RAW
      threads: 6
      type: snowflake
      user: epc_dbt
      warehouse: COMPUTE_WH
  target: dev
